name: Deploy Front

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Build Docker image
        run: |
          docker build -t us-central1-docker.pkg.dev/western-pivot-398221/felijuego/front .

      - name: Push Docker image
        run: |
          docker push us-central1-docker.pkg.dev/western-pivot-398221/felijuego/front

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy felijuego \
            --image us-central1-docker.pkg.dev/western-pivot-398221/felijuego/front \
            --platform managed \
            --region us-central1 \
            --allow-unauthenticated \
            --port 80